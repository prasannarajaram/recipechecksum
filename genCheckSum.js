function decToHex(values) {
    let hexArray = []
    for (let i=0; i<=values.length; i++) {
        console.log(values[i]);
    }
}

function genCheckSum() {
    console.log(initialValues);
    decToHex(initialValues);
}
genCheckSum();
// decToHex();
